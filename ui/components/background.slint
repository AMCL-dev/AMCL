export component Background {
    Rectangle {
        background: white;
        border-radius: 8px;
        clip: true;

        Image {
            source: @image-url("../assets/images/background.jpg");
            width: 100%;
            height: 100%;
            image-fit: cover;
        }
    }
}